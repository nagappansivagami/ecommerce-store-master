pipeline {
    agent any // Specifies that the pipeline can run on any available agent

    stages {
        stage('Print Message') { // Defines a stage named 'Print Message'
            steps {
                echo 'Hello, this is a single print statement in a Jenkins Pipeline!' // Prints the specified message to the console
            }
        }
    }
}
